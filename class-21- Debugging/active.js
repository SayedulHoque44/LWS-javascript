

/*
--------------------------------Debugger ----------------------------------
class player {
        constructor(name,age) {
                this.name = name;
                this.age = age;
                //debugger; by this debugger keyword we can give here breakpoint

        }
        play(status) {
                console.log(this.age+" aged of "+this.name+" played "+status)
        }
}

const player1 = new player("tamim",);// suppose i give here no value mistakly, so how i find it = BY DEBUGGING.
const player2 = new player("nasir",27)

player1.play("well")



*/